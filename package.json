{
  "scripts": {
    "build:shared": "node scripts/build-shared.js",
    "build:chrome": "npm run build:shared",
    "build:firefox": "npm run build:shared"
  },
  "dependencies": {
    "archiver": "^5.3.1"
  }
}
